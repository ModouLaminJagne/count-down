# count-down

A simple countdown demo app.

## Run

Open `/tmp/workspace/ModouLaminJagne/count-down/index.html` in a browser.

## Usage

- Default countdown is 3 minutes.
- Change the **Minutes** value to set a custom countdown duration.
- Click **Start** to begin and **Reset** to apply the current minutes value.
